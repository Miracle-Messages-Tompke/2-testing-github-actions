/* eslint-disable no-unused-vars */

export enum DATE_PICKER_INPUT_KIND {
  SIMPLE = "simple",
  SINGLE = "single",
  FROM = "from",
  TO = "to"
}

export enum DATE_PICKER_MODE {
  SIMPLE = "simple",
  SINGLE = "single",
  RANGE = "range"
}
