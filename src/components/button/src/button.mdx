import { Props, Story } from "@storybook/addon-docs";

# Button

## Installation

There are two ways to install and use this component, this can be done with NPM:

```sh
npm install -S @aileron/button
```

or via cdn by:

```html
<script defer src="https://cdn.aa.com/aileron/button@latest/button.js"></script>
```

## Example with usage

<Story id="components-button--default" height="160px" />

## `<adc-button>` properties

<Props of="adc-button" />
