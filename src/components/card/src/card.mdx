import { Preview, Props, Story } from "@storybook/addon-docs";

# Card

## Installation

There are two ways to install and use this component, this can be done with NPM:

```sh
npm install -S @aileron/card
```

or via cdn by:

```html
<script defer src="https://cdn.aa.com/aileron/card@latest/card.js"></script>
```

## Example with usage

<Preview withToolbar>
  <Story id="components-card--default" height="160px" />
</Preview>

## `<adc-card>` properties

<Props of="adc-card" />
