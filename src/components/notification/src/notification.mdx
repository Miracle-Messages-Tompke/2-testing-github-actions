import { Preview, Props, Story } from "@storybook/addon-docs";


# Notification

A component used for displaying content inside a notification, the building block of layout.

```sh
npm install -S @aileron/notification
```

or via cdn by:

```html
<script defer src="https://cdn.aa.com/aileron/notification@latest/notification.js"></script>
```

## Example with usage

<Preview withToolbar>
  <Story id="components-notification--default" height="160px" />
</Preview>

## `<adc-notification>` properties

<Props of="adc-notification" />
